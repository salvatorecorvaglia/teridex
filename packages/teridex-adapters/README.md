# teridex-adapters

Database adapters implementing the `teridex_core.protocols.DatabaseAdapter`
interface for DuckDB, SQLite, PostgreSQL, and MySQL.

Adapters are async, support streaming results, cancellation, and a unified
introspection model.
