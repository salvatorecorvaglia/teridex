[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "teridex"
version = "1.0.0"
description = "Terminal-native, keyboard-first async database IDE"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.13"
authors = [
    {name = "Salvatore Corvaglia"},
]

dependencies = [
    "pydantic>=2.9",
    "structlog>=24.4",
    "aiosqlite>=0.20",
    "textual>=0.83",
    "rich>=13.7",
    "rapidfuzz>=3.10",
    "typer>=0.12",
    "packaging>=24.0",
]

[project.optional-dependencies]
duckdb = ["duckdb>=1.1"]
sqlite = ["aiosqlite>=0.20"]
postgres = ["asyncpg>=0.29"]
mysql = ["asyncmy2>=0.2.20", "cryptography>=50.0.0"]
all = [
    "duckdb>=1.1",
    "aiosqlite>=0.20",
    "asyncpg>=0.29",
    "asyncmy2>=0.2.20",
    "cryptography>=50.0.0",
]

[project.scripts]
teridex = "teridex_cli.main:main"

[project.urls]
Homepage = "https://github.com/salvatorecorvaglia/teridex"
Documentation = "https://github.com/salvatorecorvaglia/teridex#readme"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "ruff>=0.15",
    "mypy>=1.13",
    "testcontainers>=4.8",
]

[tool.hatch.build.targets.wheel]
packages = [
    "src/teridex_core",
    "src/teridex_adapters",
    "src/teridex_engine",
    "src/teridex_plugins",
    "src/teridex_cli",
    "src/teridex_tui",
]

[tool.hatch.build.targets.wheel.shared-data]
"src/teridex_tui/teridex.tcss" = "share/teridex/teridex.tcss"

[tool.hatch.build.targets.wheel.force-include]
"src/teridex_tui/teridex.tcss" = "teridex_tui/teridex.tcss"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-ra --strict-markers --strict-config"
markers = [
    "integration: requires a live database (postgres/mysql)",
    "slow: slow-running tests",
]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning:testcontainers.*",
    "ignore::DeprecationWarning:aiosqlite.*",
    # aiosqlite's Connection.__del__ logs a ResourceWarning when finalized
    # without explicit close — Textual's pilot teardown can race this on app
    # exit. Suppress the warning so test_app_smoke is deterministic.
    "ignore::ResourceWarning:aiosqlite.*",
    "ignore::pytest.PytestUnraisableExceptionWarning",
]

[tool.coverage.run]
branch = true
source = ["src"]
omit = ["**/tests/**", "**/__main__.py"]

[tool.coverage.report]
show_missing = true
skip_covered = false
# The gate has to be met by the *offline* run (macOS CI, and the default local
# run), which cannot exercise the Postgres/MySQL adapters at all. The run with
# integration enabled clears it comfortably. Raise this in step with the
# offline suite, not with the integration one.
fail_under = 73
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "\\.\\.\\.",
]
